import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';

class Styles {
  static lob(double size, Color color) => GoogleFonts.lobster(
        textStyle: TextStyle(
          fontSize: size,
          color: color,
        ),
      );
}
